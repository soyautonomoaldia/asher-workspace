# 2026-06-28

- Family travel radar email delivery decision: use `ashergael68@gmail.com` as the dedicated sender and `salvadorprosper@gmail.com` as the recipient.
- Configured a separate OpenClaw MCP server `gmail-family-travel-send` for agent `main`, using `GMAIL_LOCAL_MCP_MODE=send-only` and a separate token directory at `/home/salamirin/.openclaw/private/gmail-family-travel-send`.
- Updated `gmail-local-mcp` to support configurable OAuth scopes and send-only mode so the family travel radar does not expose Gmail read tools to `main`.
- Updated family travel radar prompts and profile to use send-only delivery when authorized, with local pending-email fallback if sending is unavailable.
- Salva authorized the send-only Gmail flow. Controlled test email sent successfully from `ashergael68@gmail.com` to `salvadorprosper@gmail.com`; Gmail returned sent message ID `19f0b2e1c791f9f5`.
- Asesor fitness con Notion: localizado paquete previo en `salva_notion_import/` con Markdown e imagenes visuales de semana 1. Creado `README_ALTA_NOTION_ASESOR_FITNESS.md` y zip importable `salva_notion_import/asesor_fitness_salva_notion_import.zip`. Pendiente: importarlo desde Notion en PC porque Notion CLI no esta instalado/autenticado en este entorno.
- Asesor fitness con Notion API: creado script `salva_notion_import/scripts/create_fitness_dashboard_notion.mjs` para crear directamente una pagina `Asesor Fitness - Salva` via API con dashboard visual. Verificado con `node --check`; ejecucion real pendiente de `NOTION_API_TOKEN` y `NOTION_PARENT_PAGE` seguros.
- Corregido parser de URL de Notion en `create_fitness_dashboard_notion.mjs`: la URL `...ASESOR-FITNESS-SALVA-38cc...` se parseaba mal como UUID `a38...` al mezclar la `A` final del slug con el ID. Ahora extrae solo el ultimo segmento/ID real.
- Asesor fitness Notion: Salva detecto en app movil que el dashboard quedaba abajo, entrenos poco desarrollados y enlaces inaccesibles desde imagenes. Actualizado `create_fitness_dashboard_notion.mjs` con `NOTION_REBUILD=1` para reconstruir la pagina existente, limpiar bloques actuales y dejar dashboard arriba; entrenamientos quedan como toggles detallados con pasos y enlaces clicables.
- Asesor fitness Notion secrets: creados wrappers `setup_notion_fitness_env.sh` y `run_notion_fitness_dashboard.sh`. El setup guarda `NOTION_API_TOKEN` y `NOTION_TARGET_PAGE` fuera del workspace en `/home/salamirin/.openclaw/private/notion-fitness.env` con permisos `600`; el runner carga ese env y permite `--rebuild` sin volver a pedir/pegar token.
- Salva reporta que desde la app de Notion no ve el dashboard ni puede acceder bien al programa de ejercicios desde la imagen final. Creada version mobile-first `salva_notion_import/Asesor Fitness - Salva Dashboard Movil.md` y paquete `salva_notion_import/asesor_fitness_salva_dashboard_movil.zip`, con accesos reales arriba, enlaces de video visibles y las imagenes solo como apoyo visual.
- Tras indicar Salva que hay acceso API, revisado entorno: no hay `NOTION_API_TOKEN`, `NOTION_PARENT_PAGE`, `NOTION_TARGET_PAGE`, CLI `ntn` ni herramienta dinámica de Notion expuesta en esta sesion. Actualizado `create_fitness_dashboard_notion.mjs` para crear por API la version mobile-first con bloque `Hoy`, accesos rapidos y enlaces clicables reales; verificado con `node --check`.
