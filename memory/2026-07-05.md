## Gmail MCP attachments

- Salva asked to modify the Gmail skill/MCP so Mercury can attach PNG or PDF files when sending the AD mock.
- Updated `/home/salamirin/.openclaw/workspace/tools/gmail-local-mcp/gmail-local-mcp.mjs`: `gmail_send_message` now accepts optional `attachments` with local `path`, optional `filename`, and optional/explicit `mimeType` limited to `image/png` or `application/pdf`.
- Safety limits: max 5 attachments, 20 MB total, PNG/PDF file signature validation, existing plain-text-only behavior preserved when no attachments are passed.
- Reloaded MCP runtime cache. `openclaw mcp doctor` reports `gmail-ad: ok`, `gmail-family-travel-send: ok`, and disabled legacy `gmail-local: ok`.
- No email was sent and no Mercury/Gael coordination was performed.

## AD agent capability correction

- Salva flagged that the team capability matrix was too narrow: agents were not declaring functional needs such as web research for market/competitor/trend work, visual generation/export for UX, commercial direction for Gael, vendor/API research for Nathan, or proper source/list needs for Mercury.
- Updated persistent instructions for `saas`, `growth`, `producto`, `tecnico` and `ux`: `SOUL.md`, `IDENTITY.md`, `AGENTS.md`, `TOOLS.md` and `MEMORY.md` now require tool audits to start from each agent's real function, not from only currently visible tools.
- Updated `openclaw.json`: Gael runtime theme is now SaaS/commercial lead; Irinas runtime theme is UX/visual artifact lead; Asirin has `diagram-maker`; Nathan has `spike`; Irinas has `diagram-maker` and `spike`; Trello skill is enabled and remains assigned to Gael.
- Installed local `jq` wrapper under `~/.local/bin/jq` from Ubuntu packages because Trello skill requires `curl` + `jq` and system `jq` was missing.
- Verification: `openclaw.json` parses, gateway is active, `jq-1.7` works, Trello API read-only check succeeds, and `AD Captura Pack` is visible once. No agent coordination or external outreach was performed.

## FinOps reasoning matrix approved

- Salva approved the permanent GPT-5.5 reasoning matrix to avoid unnecessary reasoning-token spend.
- Updated live config `/home/salamirin/.openclaw/openclaw.json`: `thinkingDefault` is now `medium` for defaults, main, saas/Gael, growth/Mercury, producto/Asirin, tecnico/Nathan and ux/Irinas; subagent `thinking` is also `medium`.
- Updated persistent reasoning instructions in main and agent `SOUL.md` files with escalation levels: `none` mechanical, `low` simple low-risk work, `medium` normal work, `high` risk/gates/audits/strategy, `xhigh` only irreversible or high-impact security/legal/privacy/financial/production/credential/data-loss scenarios.
- Backup created before config change: `/home/salamirin/.openclaw/openclaw.json.bak-finops-reasoning-20260705_222258`.
- Verification: `jq empty /home/salamirin/.openclaw/openclaw.json` passed, and all listed agents report `thinkingDefault: medium` plus subagent `thinking: medium`.
