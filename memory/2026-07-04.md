# 2026-07-04

## Incidencias

- OpenClaw/Codex sufrió fallos de disponibilidad con errores de `auth refresh request timed out after 10s` y un `token_invalidated` en el flujo de Codex.
- Se limpió ruido operativo: diagnósticos colgados, servicio de sistema `openclaw.service` roto, y se verificó que `openclaw-gateway.service` seguía activo.
- Se eliminó el uso de API key OpenAI del Codex activo. El archivo `agents/main/agent/codex-home/auth.json` dejó de estar en modo `apikey`, y el binario Codex activo pasó a informar `Logged in using ChatGPT`.
- Se neutralizaron API keys OpenAI antiguas detectadas en backups/rotaciones relevantes, sin conservar valores secretos.
- Se confirmó que el error `native hook relay not found` reaparecía tras el nuevo chat. Se preparó parche reversible en `/home/salamirin/.npm-global/lib/node_modules/openclaw/dist/harness-XLdmGHJT.js` para ejecutar Codex con `nativeHookRelay: { enabled: false }` en `runAttempt` y `runSideQuestion`. Backup: `/home/salamirin/.npm-global/lib/node_modules/openclaw/dist/harness-XLdmGHJT.js.bak-disable-nativeHookRelay-20260704-135633`. Pendiente: aplicar efectivamente en próximo reinicio controlado del gateway y verificar ausencia de nuevos errores.
- Checks sin sudo: gateway activo en loopback `127.0.0.1/[::1]:18789`, disco al 22%, timers `openclaw-nightly-protection` y `openclaw-local-heartbeat` activos. Persisten procesos `openclaw-hooks`/`hooks relay` vivos consumiendo CPU moderada; no se terminaron para no cortar la sesión actual.
- Salva autorizó reinicio controlado de `openclaw-gateway.service`. Se ejecutó a las 14:10-14:11 CEST mediante unidad transitoria `openclaw-gateway-controlled-restart-20260704-141045.service`; verificación inicial: servicio activo, listener loopback y cero errores recientes del relay. Tras el reinicio la carga bajó aprox. de 6.6 GB/506 tareas a ~975 MB/103 tareas.
- Revisión posterior: hubo errores `native hook relay not found` residuales hasta aprox. 15:17 CEST, probablemente de sesiones/configuración antigua. Desde 15:30 CEST: `nativeHookRelayErrors=0` y `otherWatchedErrors=0`. Se detectó una segunda copia con relay activo en `/home/salamirin/.openclaw/npm/projects/openclaw-codex-8902d781d4__openclaw-generation__openclaw-codex-2026.6.11-openclaw-codex-2026.6.11--f9c66713f5/node_modules/@openclaw/codex/dist/harness.js`; se aplicó el mismo parche reversible con backup `harness.js.bak-disable-nativeHookRelay-20260704-153947` y `node --check` correcto.
- Salva ejecutó checks con `sudo` y guardó log en `/home/salamirin/.openclaw/logs/security-sudo-check-20260704-181525.log`. Resultado: UFW activo con entrada por defecto `deny`, salida `allow` y reglas nft/iptables gestionadas por UFW; no hay puertos externos de OpenClaw, gateway sigue en `127.0.0.1/[::1]:18789`; Timeshift tiene 5 snapshots diarios hasta `2026-07-04_16-00-02`; backups OpenClaw cifrados nocturnos existen hasta `20260704-030002`. Riesgo pendiente: disco raíz `/dev/sda2` es `ext4` y `cryptsetup status sda2` devuelve `/dev/mapper/sda2 is inactive`, por tanto no hay LUKS activo en raíz.
- El mismo log mostró que el relay no quedó completamente neutralizado: nuevos `native hook relay not found` a las 18:13 y de nuevo tras el log; procesos `openclaw-hooks` consumían CPU alta. Interpretación: el segundo parche al paquete `@openclaw/codex` se hizo después del reinicio controlado anterior, por lo que el gateway probablemente sigue usando código cargado en memoria. Pendiente recomendado: nuevo reinicio controlado del gateway, luego verificar `nativeHookRelayErrors=0` y CPU de `openclaw-hooks`.
- Antes de aplicar `doctor --fix`, se creó backup de `/home/salamirin/.openclaw/openclaw.json` en `/home/salamirin/.openclaw/openclaw.json.bak-before-doctor-fix-20260704-183435` y se validó JSON correcto. Se archivó el transcript huérfano indicado por Doctor: `/home/salamirin/.openclaw/agents/main/sessions/2cbd0183-9009-460d-9c60-d122776218ac.jsonl` -> `.jsonl.deleted.20260704-183627`.
- Warning Discord revisado: Discord debe seguir activo para Gael/equipo AD, no para Asher. Se corrigió `/home/salamirin/.openclaw/openclaw.json` para dejar `channels.discord.groupPolicy="open"` manteniendo `dmPolicy="disabled"`, servidor `1517883781615452202`, canal `1517883886506606642`, `requireMention=true` e inbound route a `agentId="saas"`. Telegram quedó en `groupPolicy="allowlist"` con allowlist de Salva. También se actualizó `tools/discord-ad/discord-ad.patch.json5` para que futuras reaplicaciones no restauren el warning. Pendiente: reinicio controlado para que el gateway cargue la configuración.
- TaskFlow recovery ejecutado con backup SQLite previo en `/home/salamirin/.openclaw/backups/taskflow-recovery-20260704-185807/openclaw.sqlite`. La CLI `openclaw tasks flow show/cancel` quedó colgada, así que se inspeccionó `/home/salamirin/.openclaw/state/openclaw.sqlite` directamente. Se confirmaron 8 flows `blocked` antiguos, todos `task_mirrored`, con `ended_at`, sin tareas hijas vinculadas y con `blocked_task_id` inexistente. Se marcaron como `cancelled` en transacción, limpiando `blocked_task_id`/`blocked_summary` y subiendo revisión. Verificación: `pragma integrity_check=ok`, `blocked_count=0`, `orphan_blocked_count=0`, `cancelled_count=10`. `openclaw doctor --non-interactive` siguió quedando en timeout antes de emitir salida; conviene reintentarlo tras el reinicio controlado pendiente del gateway.
- Reinicio controlado de `openclaw-gateway.service` ejecutado a las 19:24 CEST. El propio reinicio interrumpió la llamada de herramienta, pero al reanudar se verificó: servicio activo desde `2026-07-04 19:24:07 CEST`, PID `117731`, listener solo en `127.0.0.1/[::1]:18789`, memoria aprox. 700-870 MB y tareas 45-57. Desde el reinicio: `nativeHookRelayErrors=0`, `authRefreshTimeouts=0`, `tokenMismatch=0`, `memoryPressure=0`; no quedan procesos `openclaw-hooks` vivos. Doctor posterior ya no muestra TaskFlow recovery ni warning de Discord allowlist vacía, pero sigue agotando timeout antes de cerrar y conserva pendiente `doctor --fix` para normalizar `talk.provider/providers`. Doctor también avisa que Discord `groupPolicy="open"` permite guilds no denegadas si hay mención; revisar si se quiere endurecer con allowlist/canales tras confirmar que no rompe Gael.
- Cerrado pendiente menor de Doctor `talk.provider/providers`: backup previo `/home/salamirin/.openclaw/openclaw.json.bak-before-doctor-normalize-20260704-193112`. `openclaw doctor --fix --non-interactive` terminó OK, pero no persistió el cambio funcional porque el bloque `talk.providers.system={}` vacío seguía provocando el aviso. Se verificó con el normalizador interno de OpenClaw y se eliminó solo ese objeto vacío, manteniendo `talk.provider="system"`. Validación: JSON OK, `talk_normalized_equal=true`, Doctor ya no muestra `Normalized talk.provider/providers shape`. Quedan avisos no bloqueantes: crons informativos, memory search disabled y Discord `groupPolicy="open"` mention-gated.
