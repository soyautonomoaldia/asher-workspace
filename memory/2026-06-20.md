# 2026-06-20

## Mercury communication correction

- Salva challenged Asher for communicating with Mercury after the temporary relay permission had expired on 2026-06-15.
- Root cause: Asher interpreted "que lo verifique Mercury" as current authorization and failed to apply the stronger standing rule requiring no communication/coordination with Mercury/Gael/AD team after the temporary exception expired.
- Correction recorded in `MEMORY.md`: no contact, prompt, relay, or coordination with Mercury/Gael/AD team agents unless Salva gives explicit current authorization for the exact agent message. If there is a standing prohibition or expired permission, ask for confirmation first.

## Voice communication setup

- Enabled OpenClaw TTS under request-only mode using Microsoft Edge TTS, Spanish voice `es-ES-AlvaroNeural`.
- Enabled `talk-voice` and `microsoft` bundled plugins in `openclaw.json`; added `messages.tts` with `auto: "off"` and native `talk` config with `provider: "system"`, `speechLocale: "es-ES"`, `interruptOnSpeech: true`.
- Gateway hot reload applied the voice-related config at 2026-06-20 01:10 CEST.
- Smoke test generated a valid MP3 at `/home/salamirin/.openclaw/media/outbound/asher-voice-smoke-20260620.mp3`.
- Not enabled: phone calls via `voice-call`, external realtime voice provider, always-on automatic audio replies.
- Follow-up: WebChat/Control UI Talk realtime produced `Realtime voice provider "openai" is not configured` because browser Talk requires a realtime provider API key. Codex/OpenAI OAuth does not satisfy Realtime Voice. Config was prepared to use `talk.realtime.provider: "openai"` with `OPENAI_API_KEY` from env, model `gpt-realtime-2`, voice `cedar`, but no key is present yet. Do not paste the key in chat.

## Nightly protection

- Installed `tools/openclaw-nightly-protection.sh` to run two layers sequentially: local Git snapshots first, then encrypted OpenClaw backup.
- Installed and enabled user systemd timer `openclaw-nightly-protection.timer` for daily execution at 03:00 Europe/Madrid with `Persistent=true`.
- Manual verification at 2026-06-20 03:26 CEST succeeded: created `/home/salamirin/Backups/openclaw/nightly/openclaw-nightly-20260620-032538.tar.gz.gpg`, generated SHA256, and verified decrypt/list via GPG + tar.
- Root workspace and sub-workspaces `saas`, `producto`, `tecnico`, and `ux` now have local initial commits. No GitHub `origin` remote is configured, so pushes are skipped until Salva explicitly approves/configures a private remote.
- Correction: `growth` was initially protected through the root workspace repo, but was later split into its own local Git repo and added explicitly to the nightly Git layer.
- Correction: Asher's root workspace was initially labeled generically as `workspace` in the nightly script. It was renamed to `asher-workspace`, and `tools/openclaw-protection-manifest.md` was added to document Layer 1 vs Layer 2 coverage and the deliberate exclusion of raw runtime/auth/session state from Git.
- Backup excludes historical backups, npm payloads, Codex session directories and live Codex SQLite logs to avoid huge or unstable archives.
- Permissions hardened: `.openclaw/secrets` is `700`, the backup passphrase file is `600`, and generated nightly backups are `600`.
- Salva approved Asher's final recommendation. The backup passphrase decision remains pending and will be decided by Salva later.
- Salva created the GitHub account/repositories under `soyautonomoaldia`. Local `origin` remotes were configured for `asher-workspace`, `asher-growth`, `asher-saas`, `asher-producto`, `asher-tecnico`, and `asher-ux`.
- HTTPS push failed because no GitHub credentials were available in the runtime. Created a dedicated SSH key at `~/.ssh/openclaw_asher_github` and configured GitHub SSH usage locally. Pending: Salva must add the public key to GitHub before the first push can succeed.
- Salva added the SSH key to GitHub. SSH authentication succeeded as `soyautonomoaldia`, and first pushes completed for all six repos with `master` tracking `origin/master`.
- Second protection layer was extended from local encrypted backups to Google Drive remote sync using existing `rclone` remote `gdrive-autonomos`, destination `OpenClaw Backups/nightly`. Only encrypted `.tar.gz.gpg` files and `.sha256` checksums are uploaded; no raw OpenClaw files are copied to Drive.

## AD channel routing

- Telegram group routing experiment for Gael was cleaned up after proving brittle. `openclaw.json` now keeps Telegram routed only to Asher/main, with Salva's DM allowlist preserved and no Telegram group binding to `saas`.
- Discord was selected as the cleaner future channel for AD team operations. Prepared non-secret setup files under `tools/discord-ad/`: `README.md` and `discord-ad.patch.json5`.
- Discord is not active yet. Activation still requires Salva to create the Discord server/bot, provide non-secret IDs, and set `DISCORD_BOT_TOKEN` securely in the gateway environment without pasting it in chat.
- Salva provided Discord invite `hp97hvW6m`. Public invite metadata identified server `El servidor de AD_captura` (`1517883781615452202`) and channel `#ad_captura` (`1517883886506606642`). Installed plugin package `@openclaw/discord@2026.6.8`; updated `tools/discord-ad/discord-ad.patch.json5` with those IDs and routing `#ad_captura` -> Gael (`agentId: saas`). Still pending: Discord bot token and actual config activation.
- Salva accidentally pasted the first Discord bot token in chat; Asher instructed token reset and did not use/repeat it. Salva then set the replacement token locally in `/home/salamirin/.openclaw/gateway.systemd.env`.
- Discord config is active in `openclaw.json`: plugin `discord` enabled, token read from env, `#ad_captura` routed to Gael (`agentId: saas`), Telegram remains routed only to Asher/main.
- Gateway restart at 2026-06-20 16:33 CEST loaded Discord successfully and initialized bot client `1517892719718957236` (`openclaw AD`). Verification via Discord API confirmed the token is valid, but `/users/@me/guilds` returned an empty list: the bot has not actually been invited to the AD server yet.
- Pending next step: invite bot `openclaw AD` to server `El servidor de AD_captura` with OAuth2 permissions for reading/sending messages, then test mention in `#ad_captura`.
- Later verification after Salva's test showed the Discord bot can see server `El servidor de AD_captura` and channel `#ad_captura`. The gateway was still running from before the invite and had previously logged the channel as unresolved; final end-to-end Gael reply verification was deferred when Salva moved on to Backblaze.

## Backblaze B2 protection

- Prepared the nightly protection script to support multiple encrypted backup destinations. Existing Google Drive remote remains the default.
- Added optional `B2_BACKUP_DIR` support for a Backblaze B2 `rclone` remote, without breaking current Drive uploads.
- Added setup guide at `tools/backblaze-b2/README.md`.
- Backblaze B2 was configured with `rclone` remote `b2-openclaw` and bucket `openclaw-backups-soyautonomoaldia`.
- The nightly service now loads `/home/salamirin/.openclaw/nightly-protection.env`, which sets `B2_BACKUP_DIR=b2-openclaw:openclaw-backups-soyautonomoaldia/nightly`.
- Manual smoke test to B2 succeeded: upload/list/delete of a small test object.
- Full encrypted backup artifact `openclaw-nightly-20260620-173802.tar.gz.gpg` and `.sha256` were uploaded to Backblaze B2. Remote size was `101861858` bytes and the remote checksum file matched local SHA256 `70e48294f5b0e2bf9b177d1607b74511b8c8bb56c32c8f03436e23e6495516c2`.
- Next scheduled timer remains 2026-06-21 03:00 CEST.
