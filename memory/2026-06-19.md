# 2026-06-19

## OpenClaw post-disk-change audit

- Salva reported a hard drive change and OpenClaw update to v2026.6.8, asking for a deep audit before restarting work with full confidence.
- Host verified: Linux Mint 22.3, OpenClaw CLI/gateway v2026.6.8, gateway bound to loopback only on port 18789.
- Fixed critical OpenClaw permission findings:
  - `~/.openclaw/credentials` set to `700`.
  - Legacy per-agent `auth-profiles.json` files for saas, growth, producto, tecnico and ux set to `600`.
- Fixed plugin drift:
  - `@openclaw/acpx` and `@openclaw/codex` updated from 2026.6.6 to 2026.6.8 in managed npm projects.
  - `openclaw doctor --fix --non-interactive --yes` repaired managed npm peer links.
  - Gateway restarted and status confirmed reachable locally.
- Final security audit after fixes: 0 critical findings. Remaining warnings: local-only trusted proxy note, plugin install metadata warnings, gateway deep probe timeout despite status reachability, and plaintext gateway token in config.
- Remaining security decisions require Salva approval or interactive handling: migrate gateway token to SecretRefs, decide firewall posture, decide disk encryption/backups posture, and optionally clean/inspect blocked TaskFlows plus orphan transcripts.

## Secret migration follow-up

- Salva approved migrating secrets.
- `gateway.auth.token` was moved from plaintext in `openclaw.json` to a file-backed SecretRef using provider `localfile` and `~/.openclaw/secrets.json`.
- `openclaw secrets reload` succeeded.
- `openclaw secrets audit --json` reported `plaintextCount=0`, `unresolvedRefCount=0`; one informational legacy OAuth residue remains in SQLite and is out of scope for static SecretRef migration.
- Scrubbed plaintext gateway tokens from 37 detected `openclaw.json` backup/quarantine copies by replacing only `gateway.auth.token` with the same SecretRef.
- Verification scan found `plaintext_remaining=0` for detected `openclaw.json`/backup files and `secretref_configs=42`.

## Mercury Gmail local access

- Salva asked to replace Mercury's Gmail access path with official Google API OAuth local and remove current IMAP access from Mercury.
- Created local read-only MCP server at `tools/gmail-local-mcp/` using Gmail API scope `https://www.googleapis.com/auth/gmail.readonly`.
- Registered MCP server `gmail-local` in OpenClaw config for Codex agent `growth` only, with tool approval mode `prompt`.
- Removed `himalaya` from Mercury's configured skill list so Mercury no longer has the IMAP-based skill path.
- OAuth credentials/token storage path reserved outside the repo: `/home/salamirin/.openclaw/private/gmail-local-mcp/`.
- Salva completed local OAuth authorization; token file exists with `gmail.readonly` scope and MCP runtime cache was reloaded.
