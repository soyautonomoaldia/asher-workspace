# 2026-07-03

- 21:05 CEST: OpenClaw gateway showed a non-standard/outdated systemd service warning because the service PATH lacked `/home/salamirin/.nvm/current/bin`. Created `/home/salamirin/.nvm/current` symlink to Node `v22.23.0` and added systemd user override `openclaw-gateway.service.d/30-path.conf` with the corrected PATH. Reloaded systemd. Gateway remained active, port `18789` listened locally, and dashboard probe returned OK. Did not restart the gateway during the active chat.
- 21:xx CEST: Moved `DISCORD_BOT_TOKEN` and `OPENCLAW_TELEGRAM_BOT_TOKEN` out of the main `openclaw-gateway.service` unit and into `/home/salamirin/.openclaw/gateway.systemd.env` with mode `600`. Restarted gateway successfully; new process used corrected PATH and dashboard responded OK. Confirmed OpenAI/Codex OAuth token request timeout patch is persisted in installed dist as `TOKEN_REQUEST_TIMEOUT_MS = 3e4`.
- 21:31 CEST: Reviewed attached `openclaw doctor` output. Archived legacy `/home/salamirin/.openclaw/logs/config-health.json` to a timestamped `config-health.legacy-conflict-*.json` file with mode `600` to stop repeated migration conflict warnings. Redacted old manual backup `/home/salamirin/.openclaw/backups/manual-2026-07-03-194200/openclaw-gateway.service.txt` so it no longer contains token assignments; mode set to `600`. Remaining doctor warnings concern Discord/`saas` routing policy and should not be auto-repaired without explicit decision because they affect external messaging behavior.
