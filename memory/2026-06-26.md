# 2026-06-26

- OpenClaw update incident: after upgrade to v2026.6.10, Salva attached doctor output showing gateway initially stopped, stale service config, missing Discord plugin manifest, auth/profile migration warnings, Discord group allowlist warning, session integrity warnings, TaskFlow blocked references, and gateway connection failures.
- Reviewed attachment locally: text file, LOW local static risk, not executed, not uploaded externally.
- Verified/handled:
  - Gateway service is now active on v2026.6.10 and reachable on loopback port 18789.
  - Discord plugin manifest path that was missing in the report now exists.
  - Removed `/home/salamirin/.nvm/current/bin` from the OpenClaw gateway systemd service PATH, reloaded/restarted gateway, and confirmed `openclaw gateway status` no longer reports service PATH/config warnings.
  - Gateway logs after restart show clean startup, 8 plugins loaded, Discord and Telegram started, and interrupted main session recovered.
  - Local sessions check found no currently referenced missing transcripts in `agents/main/sessions`; orphan transcript files remain as cleanup-only historical state.
- Left unresolved/intentionally not changed:
  - Discord remains enabled with `groupPolicy="allowlist"` and no allowed group senders; do not switch to open groups without Salva approval.
  - TaskFlow blocked/missing-task warnings from doctor were not cancelled automatically; require owner review before cancelling old flows.
  - Sensitive tokens were observed in systemd environment/drop-ins; should be moved to a safer env-file/secret pattern later, without exposing values.
