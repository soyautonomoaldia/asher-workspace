# 2026-06-05

## nuevo_AD_V01 limpieza controlada

- Corregidas rutas activas de `TOOLS.md` en Gael, Asirin, Nathan e Irinas hacia `/home/salamirin/.openclaw/workspace/nuevo_AD_V01/SOURCE_OF_TRUTH_nuevo_AD_V01.md`.
- Corregido `growth/MEMORY.md` hacia la fuente activa real.
- Marcadas memorias antiguas con referencias a `ad-v1`, Tally, Drive, Sheets, piloto y reclutamiento como historicas/no operativas o como bloqueos/limites, no autorizaciones.
- Configurado Mercury con `thinkingDefault: high` y `subagents.thinking: high`.
- Configuracion mantiene a Asher sin `allowAgents`; las rondas controladas de agentes corresponden a Gael.
- `runTimeoutSeconds` subido de 180 a 300.

## Verificacion tras reinicio

- Gateway activo por systemd user, `Result=success`, `NRestarts=0`, `ExecMainPID=2020`.
- Gateway escuchando solo en loopback: `127.0.0.1:18789`, `[::1]:18789` y puerto local auxiliar `127.0.0.1:46667`.
- `openclaw gateway status --deep` confirmo runtime running, probe OK, CLI/gateway `2026.6.1`, bind loopback, auth local y 2 clientes establecidos.
- Heartbeat sigue deshabilitado.
- SSH/SSHD inactivos.
- Unattended upgrades activo y habilitado.
- No se detecto firewall gestionado con `ufw`, `firewall-cmd` o `nft`.
- Disco raiz en ext4 sin indicio de cifrado LUKS en `lsblk -f`.
- `openclaw doctor` y `openclaw security audit --deep` quedaron colgados; se terminaron los procesos de verificacion, no el gateway.

## Siguiente paso recomendado

No tocar gateway ahora. Siguiente paso: hacer una auditoria corta de seguridad local centrada en firewall disponible, cifrado de disco/backups y logs de arranque lento antes de cualquier hardening.

## Mini-auditoria local posterior

- Heartbeat: existe registro de que Salva pidio el 2026-06-03 a las 21:15 CEST paralizarlo porque bloqueaba cada 2h. Recomendacion de Asher: no reactivar el heartbeat ruidoso; sustituirlo por chequeo silencioso/manual o diario si hace falta.
- `runTimeoutSeconds=300`: correcto para auditorias y sign-off; 180 era corto, 300 no es excesivo.
- Gateway: sigue activo, `Result=success`, `NRestarts=0`, PID 2020, bind loopback, Tailscale off.
- SSH/SSHD: inactivos.
- Firewall: `ufw`, `nftables` e `iptables` estan instalados, pero la lectura real requiere sudo. No se modifico firewall porque el gateway esta en loopback y no hay servicios externos evidentes.
- Disco: raiz `ext4` sin LUKS visible. Riesgo importante si hay robo fisico; no se puede corregir en caliente.
- Backups: existen snapshots Timeshift y backup OpenClaw en `/home/salamirin/Backups/openclaw/openclaw-backup-2026-05-23-0150.tar.gz`.
- Correccion inmediata aplicada: se detectaron procesos `openclaw-hooks`/workers colgados con CPU alta; se terminaron workers no gateway. Gateway quedo activo despues del barrido.

## Heartbeat silencioso aprobado como siguiente paso

- Salva aprobo continuar con el siguiente paso.
- `HEARTBEAT.md` estaba vacio; se definio politica silenciosa sin reactivar el heartbeat automatico.
- La configuracion sigue con `heartbeat.every: 0m`.
- Se creo `tools/local-security-sudo-checks.md` con checks pendientes que requieren contrasena `sudo`.
- `sudo -n` no puede leer firewall sin contrasena; no se tocaron reglas.

## Heartbeat diario y firewall

- Salva aprobo reactivar heartbeat diario sin ruido y lanzar sudo para firewall.
- Se creo backup `/home/salamirin/.openclaw/openclaw.json.bak-heartbeat-daily-20260605-1712`.
- Se cambio `/home/salamirin/.openclaw/openclaw.json` a `agents.defaults.heartbeat.every: 24h`.
- El intento interactivo de `sudo ufw status verbose` quedo esperando contrasena; se cancelo para no dejar proceso colgado.
- Firewall no modificado. Pendiente ejecutar lectura con contrasena en terminal accesible antes de activar reglas.
- Salva ejecuto `sudo ufw status verbose` en terminal local. Resultado: UFW activo, logging low, default deny incoming, allow outgoing, routed disabled, new profiles skip. Firewall queda cerrado correctamente.
- Salva reinicio gateway a las 23:02 CEST. Verificado: config `heartbeat.every=24h`, log `heartbeat: started` con `intervalMs=86400000`, gateway activo PID 13316, `NRestarts=0`, loopback-only en `127.0.0.1:18789` y `[::1]:18789`.
# 2026-06-05

## nuevo_AD_V01 limpieza controlada

- Corregidas rutas activas de `TOOLS.md` en Gael, Asirin, Nathan e Irinas hacia `/home/salamirin/.openclaw/workspace/nuevo_AD_V01/SOURCE_OF_TRUTH_nuevo_AD_V01.md`.
- Corregido `growth/MEMORY.md` hacia la fuente activa real.
- Marcadas memorias antiguas con referencias a `ad-v1`, Tally, Drive, Sheets, piloto y reclutamiento como historicas/no operativas o como bloqueos/limites, no autorizaciones.
- Configurado Mercury con `thinkingDefault: high` y `subagents.thinking: high`.
- Configuracion mantiene a Asher sin `allowAgents`; las rondas controladas de agentes corresponden a Gael.
- `runTimeoutSeconds` subido de 180 a 300.

## Verificacion tras reinicio

- Gateway activo por systemd user, `Result=success`, `NRestarts=0`, `ExecMainPID=2020`.
- Gateway escuchando solo en loopback: `127.0.0.1:18789`, `[::1]:18789` y puerto local auxiliar `127.0.0.1:46667`.
- `openclaw gateway status --deep` confirmo runtime running, probe OK, CLI/gateway `2026.6.1`, bind loopback, auth local y 2 clientes establecidos.
- Heartbeat sigue deshabilitado.
- SSH/SSHD inactivos.
- Unattended upgrades activo y habilitado.
- No se detecto firewall gestionado con `ufw`, `firewall-cmd` o `nft`.
- Disco raiz en ext4 sin indicio de cifrado LUKS en `lsblk -f`.
- `openclaw doctor` y `openclaw security audit --deep` quedaron colgados; se terminaron los procesos de verificacion, no el gateway.

## Siguiente paso recomendado

No tocar gateway ahora. Siguiente paso: hacer una auditoria corta de seguridad local centrada en firewall disponible, cifrado de disco/backups y logs de arranque lento antes de cualquier hardening.

## Mini-auditoria local posterior

- Heartbeat: existe registro de que Salva pidio el 2026-06-03 a las 21:15 CEST paralizarlo porque bloqueaba cada 2h. Recomendacion de Asher: no reactivar el heartbeat ruidoso; sustituirlo por chequeo silencioso/manual o diario si hace falta.
- `runTimeoutSeconds=300`: correcto para auditorias y sign-off; 180 era corto, 300 no es excesivo.
- Gateway: sigue activo, `Result=success`, `NRestarts=0`, PID 2020, bind loopback, Tailscale off.
- SSH/SSHD: inactivos.
- Firewall: `ufw`, `nftables` e `iptables` estan instalados, pero la lectura real requiere sudo. No se modifico firewall porque el gateway esta en loopback y no hay servicios externos evidentes.
- Disco: raiz `ext4` sin LUKS visible. Riesgo importante si hay robo fisico; no se puede corregir en caliente.
- Backups: existen snapshots Timeshift y backup OpenClaw en `/home/salamirin/Backups/openclaw/openclaw-backup-2026-05-23-0150.tar.gz`.
- Correccion inmediata aplicada: se detectaron procesos `openclaw-hooks`/workers colgados con CPU alta; se terminaron workers no gateway. Gateway quedo activo despues del barrido.

## Heartbeat silencioso aprobado como siguiente paso

- Salva aprobo continuar con el siguiente paso.
- `HEARTBEAT.md` estaba vacio; se definio politica silenciosa sin reactivar el heartbeat automatico.
- La configuracion sigue con `heartbeat.every: 0m`.
- Se creo `tools/local-security-sudo-checks.md` con checks pendientes que requieren contrasena `sudo`.
- `sudo -n` no puede leer firewall sin contrasena; no se tocaron reglas.

## Heartbeat diario y firewall

- Salva aprobo reactivar heartbeat diario sin ruido y lanzar sudo para firewall.
- Se creo backup `/home/salamirin/.openclaw/openclaw.json.bak-heartbeat-daily-20260605-1712`.
- Se cambio `/home/salamirin/.openclaw/openclaw.json` a `agents.defaults.heartbeat.every: 24h`.
- El intento interactivo de `sudo ufw status verbose` quedo esperando contrasena; se cancelo para no dejar proceso colgado.
- Firewall no modificado. Pendiente ejecutar lectura con contrasena en terminal accesible antes de activar reglas.
- Salva ejecuto `sudo ufw status verbose` en terminal local. Resultado: UFW activo, logging low, default deny incoming, allow outgoing, routed disabled, new profiles skip. Firewall queda cerrado correctamente.
- Salva reinicio gateway a las 23:02 CEST. Verificado: config `heartbeat.every=24h`, log `heartbeat: started` con `intervalMs=86400000`, gateway activo PID 13316, `NRestarts=0`, loopback-only en `127.0.0.1:18789` y `[::1]:18789`.

## Estado al cierre de contexto 23:22 CEST

- Heartbeat diario silencioso queda operativo tras reinicio del gateway: `heartbeat.every=24h`, intervalo real 24h, con politica de silencio documentada en `HEARTBEAT.md`.
- Seguridad local queda en buen estado operativo: gateway loopback-only, UFW activo con deny incoming/allow outgoing, SSH inactivo, Tailscale apagado, unattended upgrades activo.
- Riesgo residual conocido: disco raiz ext4 sin LUKS visible; mitigado parcialmente por backups/snapshots, pero el cifrado completo requiere plan aparte y no debe hacerse en caliente.
- Regla operativa persistente para nuevo_AD_V01: Asher no coordina rondas de agentes; Gael mantiene el metodo controlado de delegacion y decision gates.
