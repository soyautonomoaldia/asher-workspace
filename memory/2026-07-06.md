## Gael Discord skill

- Salva authorized adding Discord capability to Gael/SaaS.
- Updated `/home/salamirin/.openclaw/openclaw.json`: agent `saas` skills now include `discord` and `trello`.
- Verification: `jq empty /home/salamirin/.openclaw/openclaw.json` passed; `saas` reports skills `discord`, `trello`.

## Gmail AD private batch fix

- Salva escalated that the AD Gmail MCP exposed recipients in visible tool arguments and therefore could not satisfy the approved privacy constraint for Mercury's batch send.
- Updated `/home/salamirin/.openclaw/workspace/tools/gmail-local-mcp/gmail-local-mcp.mjs`: added `gmail_send_private_batch`.
- New tool reads recipients from private server-side JSON by `batchId`, sends only when called with `execute=true` and the private batch file contains `"approved": true`, and returns only aggregate counts by P1/P2/P3.
- Updated `/home/salamirin/.openclaw/openclaw.json`: `gmail-ad` now has explicit private batch/run directories under `/home/salamirin/.openclaw/private/gmail-ad-send`.
- Added private batch README at `/home/salamirin/.openclaw/private/gmail-ad-send/batches/README.md`; no real recipients were created or sent.
- Verification: Node syntax check passed, local MCP dry-run returned aggregate counts without identifiers, `openclaw mcp doctor` reports `gmail-ad: ok`, `openclaw mcp reload` disposed cached runtimes, and `openclaw mcp probe gmail-ad` reports 5 tools.
