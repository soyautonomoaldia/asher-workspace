## Gael Discord skill

- Salva authorized adding Discord capability to Gael/SaaS.
- Updated `/home/salamirin/.openclaw/openclaw.json`: agent `saas` skills now include `discord` and `trello`.
- Verification: `jq empty /home/salamirin/.openclaw/openclaw.json` passed; `saas` reports skills `discord`, `trello`.

## Gmail AD private batch fix

- Salva escalated that the AD Gmail MCP exposed recipients in visible tool arguments and therefore could not satisfy the approved privacy constraint for Mercury's batch send.
- Updated `/home/salamirin/.openclaw/workspace/tools/gmail-local-mcp/gmail-local-mcp.mjs`: added `gmail_send_private_batch`.
- New tool reads recipients from private server-side JSON by `batchId`, sends only when called with `execute=true` and the private batch file contains `"approved": true`, and returns only aggregate counts by P1/P2/P3.
- Updated `/home/salamirin/.openclaw/openclaw.json`: `gmail-ad` now has explicit private batch/run directories under `/home/salamirin/.openclaw/private/gmail-ad-send`.
- Added private batch README at `/home/salamirin/.openclaw/private/gmail-ad-send/batches/README.md`; no real recipients were created or sent.
- Verification: Node syntax check passed, local MCP dry-run returned aggregate counts without identifiers, `openclaw mcp doctor` reports `gmail-ad: ok`, `openclaw mcp reload` disposed cached runtimes, and `openclaw mcp probe gmail-ad` reports 5 tools.

## nuevo_AD_V01 custody/preflight cron

- Cron executed and Mercury/growth session was checked.
- No `CUSTODY_FAILURE_REBUILT_READY` was found.
- Aggregate state: `CUSTODY_REBUILD_TECHNICAL_FAILURE`.
- No `batchId` exists; no private batch was created; `approved: true` is not applicable; aggregate dry-run was not executed.
- Reconstruction status: P1 recoverable; P2/P3 insufficient or not verifiable.
- Privacy status maintained: zero Gmail, zero contact, zero visible identifiers.
- Updated source of truth and Trello card `Bloqueo operativo - fallo tecnico custodia/preflight batch 5/3/2` (`https://trello.com/c/MvWuQcn0`).
- Classification remains custody/preflight, not a Salva gate, not legal/RGPD, and not `COMPLETE`.

## nuevo_AD_V01 Mercury custody final failure

- Salva reported that the last approved opportunity was executed and Mercury returned `CUSTODY_REBUILD_TECHNICAL_FAILURE_FINAL`.
- Meaning: Mercury cannot rebuild batch 5/3/2 in its current runtime without exposing identifiers.
- No private batch, no `batchId`, no `approved:true`, and no dry-run exist.
- Confirmed outcome remains zero Gmail, zero send, zero real contact, and zero visible identifiers.
- Source of truth was updated and Trello card renamed to `Fallo final custodia/preflight Mercury batch 5/3/2`.
- Technical checklist keeps only the allowed final state closed; batch/dry-run requirements remain open because they do not exist.
- Working decision: do not cancel AD on this basis, but close Mercury as operator for this external validation path under the current runtime.
- Recommended next decision: pause external send and choose between a real private custody/batch toolchain or returning to internal/documental validation without outreach.

## Mercury web/browser tool exposure

- Salva requested activating a real external search/directory/browser/API capability because Mercury reported only HTTP fetch/parsing and insufficient external signal for final P1/P2/P3.
- Updated `/home/salamirin/.openclaw/openclaw.json`: agent `growth` now has `tools.alsoAllow: ["group:web", "browser"]`.
- Existing global DuckDuckGo search and bundled browser plugin were already enabled; change exposes the installed web search/fetch group and browser automation to Mercury without changing Gmail or messaging permissions.
- Verification: `jq empty /home/salamirin/.openclaw/openclaw.json` passed and `growth` shows the new `tools.alsoAllow` block. `openclaw browser doctor` and `openclaw mcp doctor` produced no output before a 20s timeout in this shell, so runtime-level doctor remains unconfirmed.
